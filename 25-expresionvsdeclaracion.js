/* Expresión vs Declaración */

// Un operador ternario puede ser tanto una expresión como una declaración.
// En cambio, la declaración if, por ejemplo, solo es eso, una declaración. Si fuera una expresión, retornaría un valor, como lo hace el operador ternario.

// Donde JavaScript espera una sentencia, también puede escribir una expresión. Este tipo de sentencia se denomina sentencia de expresión.
// Lo contrario no se aplica: no se puede escribir una sentencia donde JavaScript espera una expresión. Por ejemplo, una sentencia if no puede convertirse
// en el argumento de una función.